package assets

import (
	"testing"
)

func TestAsset_Get(t *testing.T) {
	// 	statikFS, err := fs.New()
	// 	if err != nil {
	// 		t.Fatal(err)
	// 	}

	// 	f, err := statikFS.Open("./ground.png")

	// 	if err != nil {
	// 		t.Fatal(err)
	// 	}
	// 	img, _, err := image.Decode(f)
	// 	if err != nil {
	// 		t.Fatal(err)
	// 	}
	// 	eimg, err := ebiten.NewImageFromImage(img, ebiten.FilterDefault)
	// 	if err != nil {
	// 		t.Fatal(err)
	// 	}
	// 	ou := eimg.SubImage(image.Rect(0, 0, 16, 16))
	// ou.
	// index := 35
	// rand.Seed(time.Now().UnixNano())
	// rand := index + rand.Intn(16)

	// fmt.Println(rand, index, index-(index%16))

	// a := Assets["concrete"].Get()

	// for {
	// 	if a.index == 15 {
	// 		break
	// 	}
	// 	a = Assets["concrete"].Get()

	// 	fmt.Println(a.index)

	// }
	// fmt.Println(a.index)
	t.Fail()
}
